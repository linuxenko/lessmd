var expect = require('chai').expect;
var Events = require('../lib/events');

describe('Testing Events object', function() {

//  it('should be exist', function() {
//    var evs = new Events(process.stdin);
//
//    expect(evs).to.be.an('object');
//    expect(evs.keyExit).to.be.an('undefined');
//  });
});
